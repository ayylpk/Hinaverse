# Hinaverse 前端修改任务书（2026-08-30）

## 背景
- `frontend-Hinaverse` = 主站前端（dev 5175，Vue3 + Vite + TS + Element Plus，夜航风格：深靛蓝夜空 #141B2E / 琥珀月光 #F2B04C / 星尘紫 #B9A5E0，glass 面板，token 见 src/style.css 的 --nv-*）
- `admin-Hinaverse` = 运营台（dev 5176）
- 打卡页现状：平铺列表 + 单行输入（CheckinView.vue），日记页参考：月历视图（DiaryView.vue 的 monthGrid + calendar-panel）
- 后端无需改动（协议不变），只改前端

---

## 任务 1：打卡页改月历星星视图（frontend-Hinaverse/src/views/CheckinView.vue）

**现状**：平铺列表展示所有打卡；输入控件 `maxlength="500"`（第 156 行），**不支持换行**。

**改为（对齐 DiaryView 的月历交互）**：
1. **月历 + 星星**：月度网格仿 `DiaryView.vue`（monthGrid 生成 + 月份切换），每个日期格改成**星星造型**：
   - 星星中间显示日期数字
   - 该日**有打卡记录** → 星星**淡淡亮起**（琥珀月光色，低亮度/半透明，别太扎眼）；无记录 → 暗色描边
   - 引用现有 @keyframes 呼吸光晕风格（登录页/头像已有）保持风格统一
2. **点选与编辑**：点击某日期（或该日的星）→ 弹出当日打卡（参考日记页"点日期看当日内容"的交互），当天可新建/编辑/删除：
   - 新建/编辑控件改为**可换行的多行输入**（textarea / el-input type="textarea"），保留 `maxlength="500"` 与 "0/500" 字数提示、"2026-08-30" 日期文案
   - 保留 status ('todo' / 'done') 的现有语义与现有 CRUD 接口（`/api/checkin`），接口不变
3. 无记录的空态（如当月全空）给一句淡文案

**验收**：月历可切月；有记录的日期星星亮起（淡淡的亮）；点日期能弹出编辑并可多行输入/换行；字数上限 500 仍生效；CRUD 落库刷新后星星状态保持。

---

## 任务 2：运营台接管界面布局修复（admin-Hinaverse/src/views/TakeoverView.vue）

**现状**：消息对话区布局不稳定——点击后展示内容把框架撑开跳动，布局"反向适应内容"；消息左右规则不符预期（目前 AI 在左、用户在右）。

**改为**：
1. **框架固定，内容自适应**：对话容器固定尺寸（面板高度/宽度锁定），消息区内部滚动，任何点击/展开都不得改变整体布局（内容适应框架，不是框架适应内容）
2. **消息左右规则**（以 role 判定）：
   - `user`（用户）→ **左侧**
   - `hina`（日奈/AI）→ **右侧**
   - `system`（人工/运营）→ **右侧**（接管后人工顶替日奈原位置，继续在右侧回复用户）
   - 现有 `from-user / from-hina / from-system` 三个 class 的左右样式按上表纠正
3. 滚动锚定：消息区默认滚到最新消息，不影响固定布局

**验收**：固定布局下点击消息/操作布局不跳动；user 左、hina/system 右；接管流程 message 流按新规则显示。

---

## 任务 3：AI 反复说"人工接管"（bug）

**现象**：用户在会话中反复收到"人工接管"相关的重复提示/消息。

**排查方向**：
1. 定位提示来源：是 ws.py 人工接管中断的 system 推送（`app/ws/...` 后端）还是前端本地提示（ElMessage/系统气泡）——先定位再改，**协议/后端若需去重改动可以动**（后端不在本任务红线内）
2. 期望行为：一次接管周期内**只提示一次**（如"已转接人工"），此后不再重复；恢复/接管状态变化时才发新提示
3. 检查前端消息上屏去重（chat.ts stores 是否有重复 up 屏）

**验收**：接管后用户只收到一次接管提示；后续消息不再出现重复的"人工接管"文案。

---

## 任务 4：退出登录确认按钮错位（frontend-Hinaverse/src/views/HomeView.vue:27）

**现象**：点击"退出登录"后的 ElMessageBox 确认框**定位异常**——确认按钮跑到屏幕最左侧且**被遮挡半块**。

**改为**：确认弹窗正常居中显示、按钮完整可见。排查：
- 全局样式对 `.el-message-box`（或 popup 容器）的覆盖/transform 污染
- ElMessageBox 默认定位与夜航全局样式冲突时，改用固定定位的 el-dialog 或显式重置该弹窗样式
- 参考登录/注册弹窗（ProfileDialog 等）在项目内正常显示的写法

**验收**：退出登录 → 确认框居中、"退出/取消"按钮完整可见、点击可正常登出。

---

---

## 任务 5：运营台首管理员内置注册（部署码 + 通道关闭，含后端改动）

**背景**：当前管理员只能手工 SQL 造（注册接口恒产普通 user，无角色参数）。运营台是内部管理端，需要自助注册首个管理员；但"谁先注册谁是管理员"有恶意抢先竞态（AgentMemory 的 registerFirstUser 同样隐患，内部服务风险低暂不处理）。**定案：部署码 + 无 admin 才开放 + 注册后关通道**。（运营台公网访问控制属部署运维，不在本任务——部署时自行内网/IP 白名单。）

**后端**（backend-Hinaverse，协议新增，兼容现网）：
1. `app/config.py`：加 `ADMIN_INIT_CODE`（读 `HINA_ADMIN_INIT_CODE` 环境变量，**默认空字符串=管理员注册通道关闭**，生产必须配置强随机码 `openssl rand -hex 16` 级别；本地 .env 填 dev 码）
2. `app/routers/auth.py`：
   - 新 `GET /api/auth/admin-register-status` → `{"open": bool}`：`open = 全库无 role='admin' 且 ADMIN_INIT_CODE 非空`
   - 注册接口 body 加可选 `is_admin: bool = False`，为 True 时依次校验：
     - ① `ADMIN_INIT_CODE` 非空，否则 403「管理员注册未开放」
     - ② 请求字段 `init_code` 等于配置码，否则 400「邀请码错误」
     - ③ 全库无 role='admin'，否则 400「已存在管理员，注册通道关闭」
     - 通过才建 `role='admin'` 用户；`is_admin=false/缺省` 行为与现状完全一致（恒普通 user）
3. 测试：补 4 条 auth 单测（码对无 admin→admin 创建成功 / 码错→400 / 已有 admin→400 / 未配置码→403）

**前端**（admin-Hinaverse/src/views/LoginView.vue）：
- 挂载时 `GET /api/auth/admin-register-status`：
  - `open=true` → 登录页增加「注册管理员」切换（表单：用户名 / 密码 / 确认密码 / **管理员邀请码**必填，复用现有 400/422 detail 展示）
  - 注册成功后提示并切回登录态（或自动登录）
  - `open=false` → 只有登录，不暴露注册入口
- 用户端（frontend-Hinaverse）注册页**不动**

**验收**：① 三条件逐条后端拦截（码错 / 已有管理员 / 未配置码）② 开放时注册成功 → 再拉 status 变 false、注册入口消失 ③ 用户端注册恒 user ④ pytest 全绿 + admin-Hinaverse `pnpm build` 通过

---

## 总验收
1. 两端 `pnpm build`（vue-tsc -b）零错误通过
2. 五项分别按各自验收点手测通过
3. 任务 5/任务 3 可改后端（协议兼容新增，不改动既有字段语义），其余不动后端 API
4. 新增/修改后端逻辑需补/跑 pytest（backend-Hinaverse 既有套件 13 条）

## 不要做
- 不改风格 token、不动路由结构、不重构公共组件（除非顺手且必要）
- 不动 DiaryView 现有行为（打卡页仿它，但别反改日记页）
- 不把注册管理员通道接到用户端注册页（用户端开放注册永远只产普通用户）